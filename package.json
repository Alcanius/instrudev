{
  "name": "onepage-course-site",
  "version": "1.0.0",
  "description": "One-page course promo site with payment upload",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"No tests specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5"
  }
}
